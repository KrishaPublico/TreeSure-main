android {
    namespace 'com.example.treesure' // Replace with your actual package name
    compileSdkVersion 34

    defaultConfig {
        applicationId "com.example.treesure" // Ensure this matches the package in AndroidManifest.xml
        minSdkVersion 21
        targetSdkVersion 34
        versionCode 1
        versionName "1.0"
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = "17"
    }
}
